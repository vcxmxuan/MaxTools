# MaxTools

MaxTools 是一个只给自己使用的 macOS 26 个人工具合集。

## 环境

- macOS 26+
- Xcode 26+
- Swift 6.2+

## 运行

```sh
swift run MaxTools
```

## 设计约束

- 只使用 SwiftUI
- 使用 macOS 26 的 `glassEffect`、`GlassEffectContainer` 和 glass button style
- 不使用渐变
- 不使用黑色背景
- 图标使用单色 SF Symbols
